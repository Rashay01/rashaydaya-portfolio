import type { Metadata } from 'next'
import { notes } from '@/lib/data/notes'

export const metadata: Metadata = { title: 'Engineering Notes | Rashay Daya', description: 'Practical notes on Cloudflare, GitHub Actions, Terraform, production platforms, and Grafana.', alternates: { canonical: '/notes' } }
export default function NotesPage() { return <main id="main" className="min-h-screen px-4 pb-24 sm:px-6 md:px-10"><nav className="mx-auto flex max-w-6xl items-center justify-between py-5"><a href="/" className="logo-mark text-sm">RASHAY</a><a href="/" className="inline-flex min-h-11 items-center font-mono text-xs uppercase tracking-wider text-ash hover:text-satin">Back home</a></nav><section className="mx-auto max-w-6xl pt-16"><p className="font-mono text-xs uppercase tracking-widest text-filament">WRITING / NOTES</p><h1 className="heading-display mt-5">Engineering notes.</h1><p className="mt-6 max-w-2xl text-lg text-ash">Working notes on how I deploy, automate, secure, and observe production systems.</p><ol className="mt-16 grid gap-px overflow-hidden rounded-sm border border-ash/10 bg-ash/10 sm:grid-cols-2">{notes.map((note, index) => <li key={note.title} className="bg-card p-6 sm:p-8"><p className="font-mono text-[10px] uppercase tracking-widest text-filament">{String(index + 1).padStart(2, '0')} / {note.status}</p><h2 className="mt-7 font-calsans text-2xl text-satin">{note.title}</h2><p className="mt-4 text-sm leading-relaxed text-ash">{note.summary}</p></li>)}</ol></section></main> }
