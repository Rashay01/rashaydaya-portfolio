import type { Metadata } from 'next'
import { InnerNav } from '@/components/nav/InnerNav'
import { notes } from '@/lib/data/notes'

export const metadata: Metadata = { title: 'Engineering Notes | Rashay Daya', description: 'Practical notes on Cloudflare, GitHub Actions, Terraform, production platforms, and Grafana.', alternates: { canonical: '/notes' } }
export default function NotesPage() { return <main id="main" className="min-h-screen px-4 pb-24 sm:px-6 md:px-10"><InnerNav crumbs={[{ label: 'Home', href: '/' }, { label: 'Notes' }]} /><section className="mx-auto max-w-6xl pt-16"><p className="font-mono text-xs uppercase tracking-widest text-filament">WRITING / NOTES</p><h1 className="heading-display mt-5">Engineering notes.</h1><p className="mt-6 max-w-2xl text-lg text-ash">Working notes on how I deploy, automate, secure, and observe production systems.</p><ol className="mt-16 grid gap-px overflow-hidden rounded-sm border border-ash/10 bg-ash/10 sm:grid-cols-2">{notes.map((note, index) => <li key={note.slug} className="bg-card p-6 sm:p-8"><a href={`/notes/${note.slug}`} className="block"><p className="font-mono text-xs uppercase tracking-wide text-filament">{String(index + 1).padStart(2, '0')} / {note.status} / {new Date(note.publishedAt).toLocaleDateString('en-ZA', { year: 'numeric', month: 'short', day: 'numeric' })}</p><h2 className="mt-7 font-calsans text-2xl text-satin">{note.title}</h2><p className="mt-4 text-sm leading-relaxed text-ash">{note.summary}</p></a></li>)}</ol></section></main> }
