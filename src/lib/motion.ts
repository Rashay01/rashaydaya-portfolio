// House motion vocabulary. One easing, two speeds.
// EASE_OUT_EXPO is the JS twin of the CSS cubic-bezier(0.4, 0, 0.2, 1) family
// used in globals.css; keep them in the same spirit if either changes.
export const EASE_OUT_EXPO = [0.16, 1, 0.3, 1] as const
export const DUR_FAST = 0.35
export const DUR_BASE = 0.6
